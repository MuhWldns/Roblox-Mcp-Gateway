import EnrollPageComponent from "../routes/EnrollPage";

// Enroll renders the device enrollment page. The EnrollPage component,
// extracted from the main App route, handles the enrollment flow directly.
export default function Enroll() {
  return <EnrollPageComponent />;
}